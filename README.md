# Pedidos Amaranta (Frontend) — v2
Una sola pantalla (login arriba + formulario completo abajo). Login por **POST JSON** (nunca en la URL).
Configurar `config/config.json` con tus endpoints de GAS.
